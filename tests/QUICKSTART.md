# 🚀 Быстрый старт: User Management Tests

**1 минута до первого запуска тестов!**

---

## ⚡ 3 шага до запуска

### Шаг 1: Установить зависимости

```bash
pip install pytest pytest-asyncio
```

### Шаг 2: Перейти в директорию проекта

```bash
cd C:\Users\l0934\Projects\VoxPersona
```

### Шаг 3: Запустить тесты

```bash
pytest tests/test_user_management_fixes.py -v
```

---

## ✅ Ожидаемый результат

```
===================== 20 passed in 2.45s ======================
```

**Если видите это - всё работает! 🎉**

---

## 🎯 Быстрые команды

### Все тесты
```bash
pytest tests/test_user_management_fixes.py -v
```

### Только блокировка
```bash
pytest tests/test_user_management_fixes.py -v -m blocking
```

### Только пароли
```bash
pytest tests/test_user_management_fixes.py -v -m password
```

### Один конкретный тест
```bash
pytest tests/test_user_management_fixes.py::TestUserBlockingAndStatus::test_block_user_synchronizes_fields -v
```

---

## 🐛 Если что-то пошло не так

### Ошибка: `ModuleNotFoundError`

```bash
# Попробуйте:
python -m pytest tests/test_user_management_fixes.py -v
```

### Ошибка: `Event loop is closed`

```bash
# Обновите pytest-asyncio:
pip install pytest-asyncio --upgrade
```

### Ошибка: `pytest: command not found`

```bash
# Установите pytest:
pip install pytest
```

---

## 📚 Что дальше?

- **Подробная документация:** [README_TESTS.md](README_TESTS.md)
- **Примеры использования:** [EXAMPLES.md](EXAMPLES.md)
- **Руководство по тестированию:** [../TESTING.md](../TESTING.md)

---

## 📊 Что покрывают эти тесты?

✅ **8 тестов блокировки пользователей**
- Синхронизация статуса
- Отображение в UI
- Идемпотентность операций

✅ **10 тестов паролей**
- Генерация временных паролей (5-8 символов)
- Валидация (буквы + цифры)
- Хеширование через bcrypt

✅ **2 интеграционных теста**
- Полный lifecycle пользователя
- Проверка прав доступа

**Итого: 20 тестов, ~95% покрытие кода**

---

## 💡 Совет

**Добавьте эту команду в ваш workflow:**

```bash
# Перед каждым commit:
pytest tests/test_user_management_fixes.py -v --tb=short
```

Это сэкономит время на поиске багов! 🎯

---

**Готово! Теперь вы можете запускать тесты.** 🚀

**Последнее обновление:** 7 ноября 2025
