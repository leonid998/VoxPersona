"""
Интеграционные E2E тесты для Router Agent системы VoxPersona.

Тестирует полный workflow Router Agent:
1. Оценка релевантности 22 отчетов через Claude Haiku 4.5 API
2. Выбор оптимального индекса из 7 доступных
3. Улучшение вопроса для выбранного индекса
4. Fallback при ошибках

Тесты используют реальные API вызовы (отмечены @pytest.mark.slow).
Для быстрого запуска без API: pytest -m "not slow"

Запуск всех тестов:
    pytest tests/test_integration_e2e.py -v

Запуск только быстрых тестов:
    pytest tests/test_integration_e2e.py -v -m "not slow"

Запуск с детальным выводом:
    pytest tests/test_integration_e2e.py -v -s

Автор: fullstack-developer
Дата: 20 ноября 2025
Проект: VoxPersona Router Agent E2E Tests
"""

import asyncio
import json
import sys
import time
from pathlib import Path
from typing import Dict, List, Any
from unittest.mock import patch, AsyncMock, MagicMock

import pytest

# Добавить src в PYTHONPATH для импорта модулей
sys.path.insert(0, str(Path(__file__).parent.parent / "src"))

# Импорт тестируемых функций Router Agent
from relevance_evaluator import (
    evaluate_report_relevance,
    load_report_descriptions
)
from index_selector import (
    select_most_relevant_index,
    get_top_relevant_indices,
    validate_index_mapping,
    get_index_statistics,
    INDEX_MAPPING,
    INDEX_DISPLAY_NAMES,
    DEFAULT_INDEX
)
from question_enhancer import enhance_question_for_index

# Импорт функции загрузки описаний из run_analysis
from run_analysis import load_all_report_descriptions


# ============================================================================
# Фикстуры
# ============================================================================

@pytest.fixture
def golden_dataset() -> List[Dict[str, Any]]:
    """
    Загрузка golden dataset из файла tests/golden_dataset.json.

    Содержит 21 вопрос с ожидаемыми индексами для валидации точности Router Agent.

    Returns:
        List[Dict]: Список словарей с полями:
            - question: вопрос пользователя
            - expected_index: ожидаемый индекс
            - reasoning: пояснение выбора

    Example:
        >>> dataset = golden_dataset()
        >>> len(dataset)
        21
        >>> dataset[0]["expected_index"] in INDEX_MAPPING
        True
    """
    golden_path = Path(__file__).parent / "golden_dataset.json"

    if not golden_path.exists():
        pytest.skip(f"Golden dataset не найден: {golden_path}")

    with open(golden_path, "r", encoding="utf-8") as f:
        data = json.load(f)

    return data


@pytest.fixture
def mock_report_descriptions() -> Dict[str, str]:
    """
    Моковые описания 22 отчетов для тестов без API вызовов.

    Используется для unit-тестов и быстрой проверки логики без обращения к файловой системе.

    Returns:
        Dict[str, str]: Словарь {имя_отчета: описание}
    """
    return {
        # Dizayn (1 отчет)
        "Структурированный_отчет_аудита": "Комплексный анализ дизайна интерьера отеля...",

        # Intervyu (3 отчета)
        "Общие_факторы": "Результаты глубинных интервью с гостями отеля...",
        "Отчет_о_связках": "Анализ взаимосвязей между факторами удовлетворенности...",
        "Факторы_в_этом_заведении": "Специфические факторы конкретного заведения...",

        # Iskhodniki_dizayn (1 отчет)
        "Аудит_Дизайна": "Исходные данные аудита дизайна, замеры, фото...",

        # Iskhodniki_obsledovanie (1 отчет)
        "Обследование": "Первичные данные технического обследования инфраструктуры...",

        # Itogovye_otchety (6 отчетов)
        "Краткое резюме": "Краткое резюме комплексного обследования отеля...",
        "Ощущения": "Эмоциональные ощущения и впечатления от пребывания...",
        "Заполняемость": "Анализ заполняемости и загрузки отеля...",
        "Итоговый": "Итоговый сводный отчет по всем аспектам...",
        "Отдых": "Анализ зон отдыха и релаксации...",
        "Рекомендации": "Стратегические рекомендации по улучшению...",

        # Otchety_po_dizaynu (5 отчетов)
        "Дизайн и архитектура": "Детальный анализ дизайна и архитектурных решений...",
        "Сильные стороны": "Выявленные сильные стороны дизайна интерьера...",
        "Недостатки": "Выявленные недостатки и проблемы дизайна...",
        "Ожидания": "Анализ соответствия ожиданиям гостей...",
        "Противоречия": "Обнаруженные стилистические противоречия...",

        # Otchety_po_obsledovaniyu (5 отчетов)
        "Востребованность": "Анализ востребованности услуг и сервисов...",
        "Качество инфраструктуры": "Оценка качества инженерной инфраструктуры...",
        "Клиентский опыт": "Исследование клиентского опыта и UX...",
        "Маршруты": "Анализ маршрутов движения гостей по отелю...",
        "Обустройство": "Оценка обустройства и эргономики пространств..."
    }


@pytest.fixture
def mock_rags() -> Dict[str, Any]:
    """
    Моковый словарь из 7 RAG индексов для тестов.

    Имитирует структуру реальных RAG индексов в системе.

    Returns:
        Dict[str, Any]: Словарь с 7 индексами
    """
    return {
        "Дизайн": {"name": "Dizayn", "count": 100},
        "Интервью": {"name": "Intervyu", "count": 150},
        "Исходники дизайн": {"name": "Iskhodniki_dizayn", "count": 50},
        "Исходники обследование": {"name": "Iskhodniki_obsledovanie", "count": 75},
        "Итоговые отчеты": {"name": "Itogovye_otchety", "count": 200},
        "Отчеты по дизайну": {"name": "Otchety_po_dizaynu", "count": 180},
        "Отчеты по обследованию": {"name": "Otchety_po_obsledovaniyu", "count": 160}
    }


@pytest.fixture
def expected_indices() -> List[str]:
    """
    Список всех ожидаемых названий индексов.

    Returns:
        List[str]: 7 названий индексов Router Agent
    """
    return [
        "Dizayn",
        "Intervyu",
        "Iskhodniki_dizayn",
        "Iskhodniki_obsledovanie",
        "Itogovye_otchety",
        "Otchety_po_dizaynu",
        "Otchety_po_obsledovaniyu"
    ]


# ============================================================================
# Test 1: Полный workflow Router Agent (с реальным API)
# ============================================================================

@pytest.mark.asyncio
@pytest.mark.slow
async def test_full_router_workflow():
    """
    Тест полного цикла Router Agent: вопрос -> оценка -> выбор -> улучшение.

    Проверяет:
    - Загрузка всех 22 описаний отчетов
    - Оценка релевантности через Claude Haiku API
    - Выбор оптимального индекса
    - Улучшение вопроса для выбранного индекса

    Требует:
    - ANTHROPIC_API_KEY в .env
    - Директорию Description/Report content/ с файлами описаний

    Note:
        Тест помечен @pytest.mark.slow - пропускается с -m "not slow"
    """
    question = "проблемы с освещением в ресторане"

    # Шаг 1: Загрузка описаний отчетов
    try:
        report_descriptions = load_all_report_descriptions()
    except FileNotFoundError:
        pytest.skip("Директория с описаниями отчетов не найдена")

    assert len(report_descriptions) == 22, \
        f"Ожидалось 22 описания, загружено {len(report_descriptions)}"

    # Шаг 2: Оценка релевантности всех отчетов
    relevance = await evaluate_report_relevance(question, report_descriptions)

    # Проверка результатов оценки
    assert len(relevance) == 22, \
        f"Ожидалось 22 оценки, получено {len(relevance)}"
    assert all(0 <= score <= 100 for score in relevance.values()), \
        "Все оценки должны быть в диапазоне [0, 100]"

    # Вывод топ-5 наиболее релевантных отчетов
    top_5 = sorted(relevance.items(), key=lambda x: x[1], reverse=True)[:5]
    print(f"\n📊 Топ-5 релевантных отчетов для вопроса '{question}':")
    for rank, (name, score) in enumerate(top_5, 1):
        print(f"  {rank}. {name}: {score:.1f}%")

    # Шаг 3: Выбор наиболее релевантного индекса
    index = select_most_relevant_index(relevance, INDEX_MAPPING)

    # Проверка что выбран валидный индекс
    assert index in INDEX_MAPPING, \
        f"Выбран неизвестный индекс: {index}"
    print(f"\n✅ Выбран индекс: {index}")

    # Шаг 4: Улучшение вопроса для выбранного индекса
    enhanced = enhance_question_for_index(question, index, report_descriptions)

    # Проверка улучшенного вопроса
    assert len(enhanced) > len(question), \
        f"Улучшенный вопрос должен быть длиннее: {len(enhanced)} <= {len(question)}"

    # Проверка что ключевое слово сохранено
    question_lower = question.lower()
    enhanced_lower = enhanced.lower()

    # Хотя бы одно ключевое слово должно присутствовать
    keywords_found = any(
        keyword in enhanced_lower
        for keyword in ["освещ", "свет", "ресторан", "проблем"]
    )
    assert keywords_found, \
        f"Ключевые слова не найдены в улучшенном вопросе: {enhanced}"

    print(f"\n📝 Исходный вопрос ({len(question)} симв.): {question}")
    print(f"📝 Улучшенный вопрос ({len(enhanced)} симв.): {enhanced}")

    # Проверка длины (ожидается 150-300 символов)
    assert 50 <= len(enhanced) <= 500, \
        f"Длина улучшенного вопроса вне ожидаемого диапазона: {len(enhanced)}"


# ============================================================================
# Test 2: Тест на golden dataset (>=80% точности)
# ============================================================================

@pytest.mark.asyncio
@pytest.mark.slow
async def test_router_on_golden_dataset(golden_dataset):
    """
    Тест Router Agent на эталонном наборе из 21 вопроса.

    Проверяет:
    - Recall@3: правильный индекс в топ-3 рекомендациях >= 80%
    - Рекомендательная система работает корректно
    - Стабильность результатов

    Args:
        golden_dataset: Фикстура с эталонными вопросами и ожидаемыми индексами

    Note:
        Тест помечен @pytest.mark.slow - занимает ~60-90 секунд
        Каждый вопрос требует 22 API вызова к Claude Haiku
    """
    # Загрузка описаний отчетов
    try:
        report_descriptions = load_all_report_descriptions()
    except FileNotFoundError:
        pytest.skip("Директория с описаниями отчетов не найдена")

    correct_at_1 = 0  # Accuracy@1 - точный выбор
    correct_at_3 = 0  # Recall@3 - в топ-3
    total = len(golden_dataset)
    results = []

    print(f"\n🔄 Тестирование Router Agent (рекомендательный режим) на {total} вопросах...")

    for i, item in enumerate(golden_dataset, 1):
        question = item["question"]
        expected_index = item["expected_index"]

        # Запускаем Router Agent - получаем топ-3 рекомендации
        relevance = await evaluate_report_relevance(question, report_descriptions)
        top_indices = get_top_relevant_indices(relevance, INDEX_MAPPING, top_k=3)

        # Извлекаем названия индексов из топ-3
        top_3_names = [idx for idx, score in top_indices]
        selected_index = top_3_names[0] if top_3_names else DEFAULT_INDEX

        # Проверяем accuracy@1 и recall@3
        is_correct_at_1 = selected_index == expected_index
        is_correct_at_3 = expected_index in top_3_names

        if is_correct_at_1:
            correct_at_1 += 1
        if is_correct_at_3:
            correct_at_3 += 1

        results.append({
            "question": question,
            "expected": expected_index,
            "selected": selected_index,
            "top_3": top_3_names,
            "correct_at_1": is_correct_at_1,
            "correct_at_3": is_correct_at_3,
            "top_scores": top_indices
        })

        # Прогресс - показываем recall@3
        status = "✅" if is_correct_at_3 else "❌"
        at_1_mark = "🎯" if is_correct_at_1 else ""
        print(f"  [{i}/{total}] {status}{at_1_mark} {question[:45]}...")

    accuracy_at_1 = correct_at_1 / total * 100
    recall_at_3 = correct_at_3 / total * 100

    # Детальный отчет
    print(f"\n" + "="*60)
    print(f"📊 РЕЗУЛЬТАТЫ ТЕСТИРОВАНИЯ ROUTER AGENT")
    print(f"="*60)
    print(f"\n📈 Метрики:")
    print(f"  • Accuracy@1: {accuracy_at_1:.1f}% ({correct_at_1}/{total})")
    print(f"  • Recall@3:   {recall_at_3:.1f}% ({correct_at_3}/{total})")

    # Отчет по ошибкам (где не попали в топ-3)
    errors = [r for r in results if not r["correct_at_3"]]
    if errors:
        print(f"\n❌ Не попали в топ-3 ({len(errors)}):")
        for e in errors:
            print(f"\n  Q: {e['question']}")
            print(f"    Ожидали: {e['expected']}")
            print(f"    Топ-3: {', '.join(e['top_3'])}")
    else:
        print(f"\n🎉 Все правильные индексы в топ-3!")

    # Статистика по индексам
    print(f"\n📈 Статистика по индексам (recall@3):")
    index_stats = {}
    for r in results:
        exp = r["expected"]
        if exp not in index_stats:
            index_stats[exp] = {"total": 0, "in_top3": 0}
        index_stats[exp]["total"] += 1
        if r["correct_at_3"]:
            index_stats[exp]["in_top3"] += 1

    for idx, stats in sorted(index_stats.items()):
        idx_recall = stats["in_top3"] / stats["total"] * 100 if stats["total"] > 0 else 0
        print(f"  {idx}: {idx_recall:.0f}% ({stats['in_top3']}/{stats['total']})")

    # Проверка порога recall@3
    assert recall_at_3 >= 80, \
        f"Recall@3 Router Agent {recall_at_3:.1f}% ниже порога 80%"


# ============================================================================
# Test 3: Fallback при ошибке Router
# ============================================================================

@pytest.mark.asyncio
async def test_router_fallback_on_error(mock_report_descriptions):
    """
    Тест что при ошибке Router используется fallback поведение.

    Проверяет:
    - Graceful handling при ошибках API
    - Возврат оригинального вопроса при ошибке улучшения
    - Использование DEFAULT_INDEX при невозможности выбора

    Args:
        mock_report_descriptions: Фикстура с моковыми описаниями

    Note:
        Тест НЕ помечен @pytest.mark.slow - использует моки
    """
    question = "проблемы с дизайном интерьера"

    # Сценарий 1: Ошибка в evaluate_report_relevance (мокаем через src путь)
    with patch('src.relevance_evaluator.evaluate_single_report',
               side_effect=Exception("API Error: Connection timeout")):

        # При ошибке оценки каждого отчета все отчеты получат 0.0
        # и будет возвращен DEFAULT_INDEX
        relevance = await evaluate_report_relevance(question, mock_report_descriptions)

        # Все отчеты должны получить fallback 0.0
        assert all(score == 0.0 for score in relevance.values()), \
            "При ошибке API все оценки должны быть 0.0"
        print(f"✅ Ошибка API корректно обработана, все оценки = 0.0")

    # Сценарий 2: Ошибка в enhance_question_for_index
    with patch('src.question_enhancer.send_msg_to_model',
               side_effect=Exception("API Error")):

        # При ошибке улучшения должен вернуться оригинальный вопрос
        result = enhance_question_for_index(
            original_question=question,
            selected_index="Dizayn",
            report_descriptions=mock_report_descriptions
        )

        # Fallback: возвращается оригинальный вопрос
        assert result == question, \
            f"При ошибке должен вернуться оригинальный вопрос, получено: {result}"
        print(f"✅ Fallback на оригинальный вопрос работает корректно")

    # Сценарий 3: Пустые релевантности приводят к DEFAULT_INDEX
    empty_relevance = {
        "Неизвестный_отчет_1": 50.0,
        "Неизвестный_отчет_2": 60.0
    }

    result_index = select_most_relevant_index(empty_relevance, INDEX_MAPPING)

    # Когда все индексы имеют 0 релевантность - возвращается DEFAULT_INDEX
    assert result_index == DEFAULT_INDEX, \
        f"При нулевой релевантности должен вернуться DEFAULT_INDEX, получено: {result_index}"
    print(f"✅ DEFAULT_INDEX ({DEFAULT_INDEX}) используется при нулевой релевантности")


# ============================================================================
# Test 4: Ручной выбор индекса
# ============================================================================

@pytest.mark.asyncio
async def test_manual_index_selection(expected_indices):
    """
    Тест что все 7 индексов валидны и доступны для ручного выбора.

    Проверяет:
    - Все индексы присутствуют в INDEX_MAPPING
    - Каждый индекс имеет непустой список отчетов
    - INDEX_DISPLAY_NAMES содержит все индексы

    Args:
        expected_indices: Фикстура со списком ожидаемых индексов
    """
    print(f"\n📋 Проверка {len(expected_indices)} индексов...")

    for index in expected_indices:
        # Проверка наличия в INDEX_MAPPING
        assert index in INDEX_MAPPING, \
            f"Индекс '{index}' отсутствует в INDEX_MAPPING"

        # Проверка что список отчетов непустой
        reports = INDEX_MAPPING[index]
        assert len(reports) > 0, \
            f"Индекс '{index}' имеет пустой список отчетов"

        # Проверка наличия в INDEX_DISPLAY_NAMES
        assert index in INDEX_DISPLAY_NAMES, \
            f"Индекс '{index}' отсутствует в INDEX_DISPLAY_NAMES"

        display_name = INDEX_DISPLAY_NAMES[index]
        print(f"  ✅ {index}: {len(reports)} отчетов - {display_name}")

    # Проверка общего количества индексов
    assert len(INDEX_MAPPING) == 7, \
        f"Ожидалось 7 индексов, найдено {len(INDEX_MAPPING)}"

    print(f"\n✅ Все {len(expected_indices)} индексов валидны и готовы для выбора")


# ============================================================================
# Test 5: Производительность Router
# ============================================================================

@pytest.mark.asyncio
@pytest.mark.slow
async def test_router_performance():
    """
    Тест что Router выполняется за <= 5 секунд.

    Проверяет:
    - Общее время выполнения полного цикла Router
    - Параллельная обработка 22 запросов работает эффективно

    Note:
        Тест помечен @pytest.mark.slow - делает реальные API вызовы
        Время может варьироваться в зависимости от latency API
    """
    question = "проблемы с освещением в ресторане"

    # Загрузка описаний отчетов
    try:
        report_descriptions = load_all_report_descriptions()
    except FileNotFoundError:
        pytest.skip("Директория с описаниями отчетов не найдена")

    print(f"\n⏱️ Замер производительности Router Agent...")

    start = time.time()

    # Полный цикл Router Agent
    # Этап 1: Оценка релевантности (параллельно 22 запроса)
    relevance = await evaluate_report_relevance(question, report_descriptions)

    # Этап 2: Выбор индекса (локальная операция, мгновенно)
    index = select_most_relevant_index(relevance, INDEX_MAPPING)

    # Этап 3: Улучшение вопроса (1 запрос к API)
    enhanced = enhance_question_for_index(question, index, report_descriptions)

    elapsed = time.time() - start

    print(f"\n📊 Результаты производительности:")
    print(f"  Время выполнения: {elapsed:.2f} сек")
    print(f"  Оценено отчетов: {len(relevance)}")
    print(f"  Выбран индекс: {index}")
    print(f"  Длина улучшенного вопроса: {len(enhanced)} символов")

    # Проверка времени (<=5 секунд)
    # Примечание: на медленных сетях может занять больше времени
    # Порог 5 секунд - оптимистичный, для production рекомендуется 10 секунд
    if elapsed > 5.0:
        print(f"\n⚠️ Время выполнения {elapsed:.2f}s превышает целевое 5.0s")
        print("   Это может быть вызвано высокой latency API")

    # Мягкая проверка (warning вместо fail при превышении)
    assert elapsed <= 10.0, \
        f"Router критически медленный: {elapsed:.2f} сек > 10.0 сек"

    if elapsed <= 5.0:
        print(f"\n✅ Производительность в норме: {elapsed:.2f}s <= 5.0s")


# ============================================================================
# Test 6: Все отчеты покрыты маппингом
# ============================================================================

def test_all_reports_mapped():
    """
    Тест что все 22 отчета покрыты маппингом INDEX_MAPPING.

    Проверяет:
    - Общее количество отчетов = 22
    - Нет дублирующихся отчетов между индексами
    - Распределение отчетов соответствует ожидаемому
    """
    all_reports = set()
    report_list = []

    for index_name, reports in INDEX_MAPPING.items():
        for report in reports:
            report_list.append(report)
            all_reports.add(report)

    # Проверка общего количества
    assert len(all_reports) == 22, \
        f"Ожидалось 22 уникальных отчета, получено {len(all_reports)}"

    # Проверка на дубликаты
    duplicates = [r for r in report_list if report_list.count(r) > 1]
    assert len(duplicates) == 0, \
        f"Обнаружены дублирующиеся отчеты: {set(duplicates)}"

    # Проверка распределения по индексам
    expected_distribution = {
        "Dizayn": 1,
        "Intervyu": 3,
        "Iskhodniki_dizayn": 1,
        "Iskhodniki_obsledovanie": 1,
        "Itogovye_otchety": 6,
        "Otchety_po_dizaynu": 5,
        "Otchety_po_obsledovaniyu": 5
    }

    actual_distribution = get_index_statistics()

    assert actual_distribution == expected_distribution, \
        f"Распределение отчетов не соответствует ожидаемому:\n" \
        f"Ожидалось: {expected_distribution}\n" \
        f"Получено: {actual_distribution}"

    print(f"\n✅ Все 22 отчета корректно распределены по 7 индексам:")
    for idx, count in sorted(actual_distribution.items()):
        print(f"  {idx}: {count} отчетов")


# ============================================================================
# Test 7: INDEX_DISPLAY_NAMES соответствует INDEX_MAPPING
# ============================================================================

def test_display_names_consistency():
    """
    Тест что INDEX_DISPLAY_NAMES содержит все индексы из INDEX_MAPPING.

    Проверяет:
    - Все индексы из INDEX_MAPPING имеют display name
    - Нет лишних индексов в INDEX_DISPLAY_NAMES
    - Все display names непустые строки
    """
    mapping_indices = set(INDEX_MAPPING.keys())
    display_indices = set(INDEX_DISPLAY_NAMES.keys())

    # Проверка что все индексы из MAPPING есть в DISPLAY_NAMES
    missing_in_display = mapping_indices - display_indices
    assert len(missing_in_display) == 0, \
        f"Индексы отсутствуют в INDEX_DISPLAY_NAMES: {missing_in_display}"

    # Проверка что нет лишних индексов в DISPLAY_NAMES
    extra_in_display = display_indices - mapping_indices
    assert len(extra_in_display) == 0, \
        f"Лишние индексы в INDEX_DISPLAY_NAMES: {extra_in_display}"

    # Проверка что все display names непустые
    for index, display_name in INDEX_DISPLAY_NAMES.items():
        assert isinstance(display_name, str), \
            f"Display name для '{index}' не является строкой: {type(display_name)}"
        assert len(display_name.strip()) > 0, \
            f"Display name для '{index}' является пустой строкой"

    print(f"\n✅ INDEX_DISPLAY_NAMES корректно соответствует INDEX_MAPPING:")
    for index in sorted(INDEX_MAPPING.keys()):
        print(f"  {index} -> {INDEX_DISPLAY_NAMES[index]}")


# ============================================================================
# Test 8: Валидация INDEX_MAPPING
# ============================================================================

def test_index_mapping_validation():
    """
    Тест валидности структуры INDEX_MAPPING.

    Проверяет:
    - INDEX_MAPPING проходит validate_index_mapping()
    - Нет пустых списков отчетов
    - Нет дублирующихся отчетов
    """
    # Используем встроенную функцию валидации
    is_valid = validate_index_mapping(INDEX_MAPPING)

    assert is_valid, "INDEX_MAPPING не прошел валидацию validate_index_mapping()"

    print(f"\n✅ INDEX_MAPPING прошел валидацию структуры")


# ============================================================================
# Test 9: Тест с моками (без API вызовов)
# ============================================================================

@pytest.mark.asyncio
async def test_router_workflow_mocked(mock_report_descriptions):
    """
    Тест workflow Router Agent с моками (без реальных API вызовов).

    Проверяет:
    - Корректность логики выбора индекса
    - Обработка результатов оценки релевантности
    - Интеграция между модулями

    Args:
        mock_report_descriptions: Фикстура с моковыми описаниями

    Note:
        Быстрый тест - не делает реальных API вызовов
    """
    question = "проблемы с дизайном интерьера"

    # Мок релевантностей (высокие для отчетов по дизайну)
    mock_relevance = {
        # Otchety_po_dizaynu - высокие оценки
        "Дизайн и архитектура": 95.0,
        "Сильные стороны": 90.0,
        "Недостатки": 88.0,
        "Ожидания": 85.0,
        "Противоречия": 83.0,

        # Dizayn - средняя оценка
        "Структурированный_отчет_аудита": 70.0,

        # Остальные - низкие оценки
        "Обследование": 25.0,
        "Краткое резюме": 30.0,
        "Общие_факторы": 20.0
    }

    # Вместо мока API напрямую вызываем select_most_relevant_index
    # с подготовленными данными релевантности
    selected_index = select_most_relevant_index(mock_relevance, INDEX_MAPPING)

    # Otchety_po_dizaynu имеет 5 отчетов со средней релевантностью:
    # (95 + 90 + 88 + 85 + 83) / 5 = 88.2
    # Dizayn имеет 1 отчет: 70.0
    # Остальные индексы имеют 0 (нет отчетов в mock_relevance)

    # Проверка что выбран Otchety_po_dizaynu (наивысшая средняя релевантность)
    assert selected_index == "Otchety_po_dizaynu", \
        f"Ожидался Otchety_po_dizaynu, получено {selected_index}"

    print(f"\n✅ Мок тест пройден: выбран индекс {selected_index}")


# ============================================================================
# Test 10: Проверка граничных случаев
# ============================================================================

def test_edge_cases():
    """
    Тест граничных случаев и edge cases.

    Проверяет:
    - Обработка одинаковых релевантностей (детерминированность)
    - Обработка только одного отчета с оценкой
    - Обработка минимальных/максимальных значений
    """
    # Случай 1: Равные релевантности - должен выбраться первый по алфавиту
    equal_relevance = {
        "Структурированный_отчет_аудита": 50.0,  # Dizayn
        "Общие_факторы": 50.0,  # Intervyu
        "Отчет_о_связках": 50.0,  # Intervyu
        "Факторы_в_этом_заведении": 50.0  # Intervyu
    }

    result1 = select_most_relevant_index(equal_relevance, INDEX_MAPPING)
    # Dizayn: 50.0 (1 отчет), Intervyu: 50.0 (3 отчета)
    # При равенстве выбирается первый по алфавиту
    assert result1 in ["Dizayn", "Intervyu"], \
        f"Неожиданный результат при равных релевантностях: {result1}"
    print(f"✅ Случай 1 (равные релевантности): {result1}")

    # Случай 2: Только один отчет
    single_report = {"Обследование": 80.0}
    result2 = select_most_relevant_index(single_report, INDEX_MAPPING)
    assert result2 == "Iskhodniki_obsledovanie"
    print(f"✅ Случай 2 (один отчет): {result2}")

    # Случай 3: Минимальные значения (но не нулевые)
    minimal_relevance = {
        "Краткое резюме": 0.1,
        "Итоговый": 0.1,
        "Рекомендации": 0.1
    }
    result3 = select_most_relevant_index(minimal_relevance, INDEX_MAPPING)
    assert result3 == "Itogovye_otchety"
    print(f"✅ Случай 3 (минимальные значения): {result3}")

    # Случай 4: Максимальные значения
    max_relevance = {
        "Дизайн и архитектура": 100.0,
        "Сильные стороны": 100.0
    }
    result4 = select_most_relevant_index(max_relevance, INDEX_MAPPING)
    assert result4 == "Otchety_po_dizaynu"
    print(f"✅ Случай 4 (максимальные значения): {result4}")

    print(f"\n✅ Все граничные случаи обработаны корректно")


# ============================================================================
# Точка входа для запуска тестов
# ============================================================================

if __name__ == "__main__":
    """
    Запуск тестов напрямую через Python.

    Example:
        python tests/test_integration_e2e.py
        python tests/test_integration_e2e.py -v
        python tests/test_integration_e2e.py -v -m "not slow"
    """
    pytest.main([__file__, "-v", "--tb=short", "-s"])
